# Secrets — never commit
.env
.env.local

# Generated artifacts
catalan_deck.apkg
*.apkg

# Cached media downloads (regeneratable)
media/

# Python
__pycache__/
*.pyc
.venv/


# ---------------------------------------------------------------------------
# Material con copyright — NO redistribuible
# ---------------------------------------------------------------------------
# La transcripción completa de "A Punt A1+ A2" (227 páginas, ~555.000
# caracteres) es la obra entera, no una cita. Uso personal únicamente.
libros/

# Citas del libro, extraidas del contenido publico
content/private/

# Cache de respuestas de la API (regenerable, no aporta al repo)
.cache/

# Glosario completo de la AVL (1.359 entradas): es la obra entera, no un
# extracto. Los ~40 terminos que usa el mazo estan en content/themes/coral.tsv
# con su atribucion.
content/sources/avl-musica-full.tsv
